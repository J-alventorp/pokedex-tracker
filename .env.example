# Optional. Get a free key at https://pokemontcg.io/ for higher API rate limits.
# Only used by `npm run fetch-data` when regenerating the local card dataset —
# the deployed app itself never calls the live API.
VITE_POKEMONTCG_API_KEY=
